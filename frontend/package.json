{
  "name": "frontend",
  "version": "1.0.0",
  "description": "My astounding Nuxt.js project",
  "author": "h.ono",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate && cpx \"dist/**/*\" ../src/main/resources/static"
  },
  "dependencies": {
    "nuxt": "^2.15.8"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
